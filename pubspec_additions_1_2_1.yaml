name: equalizer_pro
description: Equalizer Pro - Responsive Flutter audio workspace
publish_to: 'none'
version: 1.2.0+3

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  # --- Existing deps (จากโปรเจคเดิม) ---
  flutter_soloud: ^2.0.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  file_picker: ^8.0.0
  desktop_drop: ^0.4.5
  audio_session: ^0.1.18
  
  # --- NEW: Markdown Runtime for PR #3 ---
  flutter_markdown: ^0.7.4+1
  cached_network_image: ^3.3.1
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
